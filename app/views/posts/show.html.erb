<div class="container">

	<div class="image-bar">
		<div class="card" style="width: 70rem;">
			<%= image_tag "#{@post.image_name}" %>
			<div class="card-body">
				<h5 class="card-title"><%= @post.name %></h5>
				<p class="card-text"><%= @post.introduce %></p>
			</div>
			<ul class="list-group list-group-flush">
				<li class="list-group-item"><%= @post.address %></li>
			</ul>
			 <div class="card-body">
				<%= link_to "予約画面へ", new_post_reservation_path(@post) , class: "btn btn-primary" %>
			</div>
		</div>
	</div>

</div>